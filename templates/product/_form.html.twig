<div class="container">
    {{ form_start(form) }}
    <div class="row">
        {{ form_widget(form) }}
    </div>

    <button class="btn btn-outline-primary btn-block">
        {{ button_label|default('Save') }}
    </button>
    {{ form_end(form) }}
</div>
